export const enum RoutePaths {
  Root = "/",
  SingleMarker = "/single-marker",
  CitiesMarkers = "/cities-markers",
  MarkersWithTooltip = "/markers-with-tooltip",
  RadiusFilter = "/radius-filter",
}
